# One-time bridge for the known project provisioned through Supabase MCP.
# Its verified schema already matches V1-V3. Do not enable on arbitrary databases.
spring:
  flyway:
    baseline-on-migrate: true
    baseline-version: '3'
